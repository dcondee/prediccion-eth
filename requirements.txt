# =============================================================================
# requirements.txt — ETH/USDT Price Predictor
# Pinned versions para reproducibilidad en producción
# =============================================================================

# ── Core científico ──────────────────────────────────────────────────────────
numpy==1.26.4
pandas==2.2.1
scipy==1.12.0

# ── Machine Learning ─────────────────────────────────────────────────────────
scikit-learn==1.4.1
joblib==1.3.2

# ── Series temporales y finanzas ─────────────────────────────────────────────
timeseries-cv                # Validación cruzada temporal (CORE del proyecto)
statsmodels==0.14.1          # Tests estadísticos: ADF, Ljung-Box, BDS
arch==6.3.0                  # GARCH, test LM, modelos de volatilidad
ta==0.11.0                   # Indicadores técnicos de trading

# ── API y servidor ───────────────────────────────────────────────────────────
fastapi==0.110.0
uvicorn[standard]==0.27.1    # [standard] incluye uvloop, httptools, websockets
pydantic==2.6.3
python-multipart==0.0.9      # Para uploads de archivos en FastAPI

# ── HTTP y conectividad ──────────────────────────────────────────────────────
requests==2.31.0
httpx==0.27.0                # Cliente HTTP async (para tests de la API)

# ── Serialización y persistencia ─────────────────────────────────────────────
pyarrow==15.0.0              # Parquet I/O para datasets
fastparquet==2024.2.0

# ── Visualización (notebooks) ────────────────────────────────────────────────
matplotlib==3.8.3
seaborn==0.13.2

# ── Testing y calidad de código ──────────────────────────────────────────────
pytest==8.1.0
pytest-asyncio==0.23.5
pytest-cov==4.1.0
httpx==0.27.0                # Para TestClient de FastAPI en tests
flake8==7.0.0
flake8-docstrings==1.7.0
black==24.2.0
isort==5.13.2
mypy==1.8.0

# ── Utilidades ───────────────────────────────────────────────────────────────
python-dotenv==1.0.1         # Variables de entorno desde .env
tqdm==4.66.2                 # Barras de progreso
